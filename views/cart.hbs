<section id="cart">
  <div class="container mt-5">
    <h4>Vanguard <span class="vl">|</span> {{title}}</h4>
    <hr>
    <div class="row pt-6">
      {{#if products}}
        <div class="col-7">
          {{#each products}}
            <div class="row py-3">
              <div class="col-3">
                <a href="/product_details/{{this.slug}}">
                  <img class="img-fluid" src="{{this.image}}">
                </a>
              </div>
              <div class="col-8">
                <a href="/product_details/{{this.slug}}">
                  <h5><strong>{{this.name}}</strong></h5>
                </a>
                <p class="mb-5">Quantity: {{this.qty}}</p>
                <div class="row">
                  <div class="col">
                    <p class="float-start">₱{{this.subPrice}}</p>
                  </div>
                  <div class="col">
                    <a href="/delete_from_cart/{{this.id}}" class="float-end"><span class="material-icons">remove_shopping_cart</span></a>
                  </div>
                </div>
              </div>
            </div>
          {{/each}}
        </div>
        <div class="col-2"></div>
        <div class="col-3" id="CartBreakdown">
          <h4>Order Summary</h4>
          {{#each products}}
            <div class="row">
              <div class="col">
                {{this.name}}
              </div>
              <div class="col text-end pe-5">
                ₱{{this.subPrice}}
              </div>
            </div>
          {{/each}}
          <div class="row pt-3">
            <hr>
          </div>
          <div class="row">
            <div class="col">
              <h5>Total:</h5>
            </div>
            <div class="col text-end pe-5">
              ₱{{total}}
            </div>
          </div>
          <div class="row pt-5 pe-5">
            <div class="container text-end">
              <a href="/billing" class="btn btn-secondary">Checkout</a>
            </div>
          </div>    
        </div>
      {{else}}
        <h4 class="mb-5 text-center">Your shopping cart is empty</h4>
        <div class="container text-center">
          <a href="/marketplace" class="btn btn-secondary">Go to Marketplace</a>
        </div>
      {{/if}}
    </div>
  </div>
</section>